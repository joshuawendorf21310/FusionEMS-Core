name: Deploy Backend
on:
  workflow_dispatch:
  workflow_call:
  push:
    branches: ["main"]
    paths:
      - 'backend/**'
      - '.github/workflows/deploy-backend.yml'
permissions:
  id-token: write
  contents: read
jobs:
  deploy-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Backend deploy pipeline placeholder (build/push image + ECS rollout)."
