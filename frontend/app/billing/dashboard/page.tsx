export default function Page() {
  return (
    <div className="rounded-2xl border border-border bg-panel p-6">
      <div className="text-lg font-semibold">Billing Dashboard</div>
      <div className="mt-2 text-sm text-muted">Revenue metrics + claims worklist will bind to APIs.</div>
    </div>
  );
}
