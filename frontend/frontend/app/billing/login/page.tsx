export default function Page() {
  return (
    <div className="rounded-2xl border border-border bg-panel p-6">
      <div className="text-lg font-semibold">Billing Login</div>
      <div className="mt-2 text-sm text-muted">Login is handled by backend auth; UI shell placeholder.</div>
    </div>
  );
}
